server:
  port: 8002
spring:
  application:
    name: movie-service
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/cts_movie
    username: root
    password: root
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/  